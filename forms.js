function scrollToRegisterForm(){
    var targetDiv = document.querySelector(".register_form");
    targetDiv.scrollIntoView({ behavior: "smooth"});
}
function scrollToBooksTable(){
    var targetDiv = document.querySelector(".books");
    targetDiv.scrollIntoView({ behavior: "smooth"});
}